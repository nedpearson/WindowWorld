# Production environment — set by Railway at build time
# VITE_API_URL is injected by Railway as a service variable
# pointing to the windowworld-api Railway service URL

VITE_APP_NAME=WindowWorld
VITE_APP_ENV=production
