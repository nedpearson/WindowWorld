{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
          "builder": "NIXPACKS",
          "buildCommand": "cd .. && npm install --legacy-peer-deps && cd server && npx prisma generate"
    },
    "deploy": {
          "startCommand": "npx prisma db push --accept-data-loss && npx prisma db seed && node dist/index.js",
          "healthcheckPath": "/health",
          "healthcheckTimeout": 120,
          "restartPolicyType": "ON_FAILURE",
          "restartPolicyMaxRetries": 3
    }
}
